# État actuel — Boutique de Boxe

## Passe du 16 septembre 2026 — hero, parcours, accessibilité et domaine (corrigé le 17 : .com)

**Cette section remplace les anciennes consignes de domaine et d’hébergement.** La demande actuelle est de raffiner la version existante, pas de rétablir une V2. Le dépôt Git réel est `C:\Users\Mommy Jayce\Desktop\Boxing Center\Deployment\boutique-de-boxe\site`, base de travail `f274e54`. La pile constatée est Next.js 16.3.4, Postgres / Supabase Auth et déploiement prévu sur Vercel. L’ancien aperçu Sites ne représente pas ce code.

**Décision du propriétaire (17 septembre 2026) : `https://boutique-de-boxe.com` est l’origine SEO/GEO publique.** Le `.fr` posé le 16 était une erreur, corrigée le 17. `shop.origin` et `shop.intendedDomain` sont fixes dans `lib/catalog.ts` ; ni un aperçu ni une variable d’environnement périmée ne changent les canonicals.

### Ce qui a changé

- **Hero :** photo réelle plus grande ; cadrage propre de « L’autre face », sans la marge grise intégrée à la photo ; zoom de la manchette dissocié du léger mouvement de pointeur. Une collision de spécificité CSS cumulait les deux zooms : corrigée après inspection visuelle. Aucun détail photographique n’a été inventé. Les boutons ont un état sélectionné explicite et le lien porte « Voir ce modèle » avec son nom de produit. Le mode animations réduites conserve le cadrage statique.
- **Pied de page :** un accès dominant au catalogue, puis deux choix utiles — préparer son sac et comprendre les onces. Hiérarchie encre/crème/citron conservée. Les neuf familles, guides, services, contact, mentions légales, cookies et préférence d’animation sont accessibles. Les vingt intentions SEO sont conservées en HTML dans un répertoire natif dépliable ; le contenu n’est pas remplacé par une liste de mots-clés décorative.
- **Accessibilité / UX :** refus des cookies plus lisible et cible de 48 px ; arrière-plan `inert` pendant le dialogue, boucle clavier et restitution du focus ; lecture du consentement au serveur pour supprimer la divergence d’hydratation constatée ; secours `<noscript>` qui retire le voile et libère le contenu ; ancres dégagées sous le header ; révélation d’une section recevant le focus. Le header et les noms de familles ne débordent plus à 320 px. Les contrôles du hero font au moins 52 px (56 px sur mobile), ses liens 44 px.
- **Exactitude :** « achats fréquents » supprimé de l’accueil faute de données de ventes. L’état d’ouverture reste explicite et les prix restent prévisionnels.
- **SEO / GEO :** canonicals, hreflang, Open Graph, Twitter, JSON-LD, sitemap, robots, flux catalogue, MCP et fichiers agents dépendent de l’origine `.fr`. Nom alternatif du WebSite, `security.txt`, indication Search Console de l’atelier et exemples d’environnement corrigés. `urlOf` résout maintenant correctement les photos externes : l’ancien code concaténait deux origines dans le sitemap et les flux machine.
- **Aucune redirection d’hôte** (demande explicite du propriétaire, 17 septembre) : `www`, l’alias stable Vercel et l’apex servent tous le site tel quel ; les canonicals, le plan de site, le graphe et les flux pointent vers `https://boutique-de-boxe.com`, ce qui suffit aux moteurs pour consolider sur le domaine.
- **Paiement :** `.env.local`, `.env.vercel`, `.env.example` sont en `COMMERCE_MODE=simulation`. Les deux variables publiques d’origine sont en `.fr`. L’adaptateur accepte uniquement les deux origines `.fr` prévues ; les anciennes origines et URL piégées sont testées comme refusées. Le verrou de paiement réel reste fermé. Aucune clé privée modifiée ou affichée, aucun paiement ni e-mail envoyé.

### Application de la barre de qualité

Conserver le langage visuel validé et améliorer ce qui sert la décision : montrer la matière et le produit, rendre les chemins de lecture explicites, donner un poids visuel différent aux actions principales et secondaires. Les références déjà analysées restent l’inspection d’objet de Teenage Engineering, le détail matériel de Vollebak et la hiérarchie éditoriale d’A24. Pas de nouvelle collection de composants génériques, de bandeau animé automatique ni de promesse commerciale ajoutée pour habiller l’interface.

La critique s’appuie sur le rendu et les usages : grand écran, petit écran, clavier, réduction des animations, métadonnées et liens réels. Les défauts découverts pendant la recette ont été corrigés, y compris le double zoom, le débordement à 320 px, l’hydratation du consentement et les URL d’images concaténées. Aucun pourcentage arbitraire ni promesse de première position. Cette passe n’est pas une certification WCAG complète, ni une preuve de Core Web Vitals terrain ou de disponibilité du domaine public.

### Vérifications de cette passe

- Build Next.js de production : réussi après les dernières corrections ; contrôle TypeScript intégré réussi.
- Lint oxlint : réussi. `git diff --check` : aucune erreur de blancs.
- Audit SEO sur le build de production local (port 3001) : **1 104 pages, 1 104 titres uniques, 1 104 descriptions uniques, 2 440 liens internes relevés** ; contrôles H1, métadonnées, graphes, origines `.fr`, mots-clés du brief, fichiers agents, absence d’offres commerciales inventées, pages privées non indexables et vignettes sociales passés. Rapport détaillé : `outputs/seo.json` (artefact local, ignoré par Git).
- PayPlug : **19/19 contrôles avec réponses simulées**, y compris les anciennes origines refusées, **zéro requête externe**. Trace locale : `outputs/payplug-2026-09-16.log`.
- `QA_ORIGIN=http://localhost:3001 node scripts/test-public-shell.mjs` : quatre états de cookie rendus par le serveur (absent, accepté, refusé, invalide), secours no-JS présent dans le HTML, canonique `.fr`, redirections 308 www et alias Vercel avec chemin/query conservés, absence de boucle sur le domaine principal : réussi.
- Navigateur : hero et pied de page inspectés sur ordinateur (jusqu’à 1440 px) et sur mobile (390 et 320 px) ; aucun débordement horizontal à 320 px ; accès catalogue et guide effectivement parcourus ; ancre du sac vérifiée sous le header. La vue « L’autre face » charge sa photo réelle et reste agrandie. Les petites oscillations du pointeur sont coupées quand les animations sont réduites ; le zoom statique reste visible.
- Sur le build compilé : ouverture du consentement au clavier, arrière-plan `inert`, Shift+Tab vers le dernier lien, refus puis restitution du focus au bouton Cookies, zéro nœud `inert` résiduel. Aucun message console d’erreur/avertissement rattaché au port 3001 lors de ce parcours ; les anciens messages HMR de développement ne sont pas présentés comme des erreurs de production.
- Limites : secours sans JavaScript vérifié par le HTML, pas par une session navigateur avec JavaScript désactivé ; aucune recette exhaustive avec lecteur d’écran ni certification WCAG ; aucune mesure de trafic ou de classement, aucun test de paiement distant ou de réception d’e-mail dans cette passe.

### Déploiement et suite concrète

Le code et les exemples locaux sont prêts pour `boutique-de-boxe.com`. **Aucun déploiement ni changement DNS / Vercel / Supabase distant n’a été exécuté dans cette passe.** La sonde Web sur le domaine a été bloquée par l’outil ; sa disponibilité publique et son HTTPS restent non vérifiés. Les anciennes constatations de déploiement ne constituent pas une preuve actuelle.

1. Ajouter `boutique-de-boxe.com` et `www.boutique-de-boxe.com` au projet Vercel, poser les DNS indiqués et vérifier HTTPS.
2. Déployer cette version avec `NEXT_PUBLIC_SITE_ORIGIN=https://boutique-de-boxe.com`, `PAYPLUG_PUBLIC_BASE_URL=https://boutique-de-boxe.com`, `COMMERCE_MODE=simulation`. Les deux fichiers `.env.local` et `.env.vercel` restent ignorés par Git ; ne jamais les ajouter.
3. Configurer Supabase Auth : Site URL `https://boutique-de-boxe.com`, callback `https://boutique-de-boxe.com/api/auth/callback`, conserver l’URL locale utile au développement. Contrôler un lien magique sur le domaine raccordé dans une recette séparée.
4. Vérifier en public le sitemap, robots, un produit, un guide, la vignette sociale et les fichiers agents (aucune redirection d’hôte n’est attendue). Vérifier ensuite l’IPN et les retours dans un test PayPlug explicitement autorisé.
5. Propriété Search Console : `boutique-de-boxe.com`, sitemap `https://boutique-de-boxe.com/sitemap.xml`. Le propriétaire garde la décision d’indexation ; aucun IndexNow ni envoi Search Console n’a été déclenché.

Procédures détaillées actualisées : [DEPLOY-VERCEL.md](DEPLOY-VERCEL.md), [PAYPLUG-WIRING.md](PAYPLUG-WIRING.md), [SEO-GEO.md](SEO-GEO.md). Scripts utiles : `scripts/audit-seo.mjs`, `scripts/test-public-shell.mjs`, `scripts/test-payplug.mjs`.

### Dossiers de contexte conservés

- `C:\Users\Mommy Jayce\Desktop\Boxing Center\Deployment\boutique-de-boxe` : brief, recherche et sous-dossier applicatif `site`.
- `C:\Users\Mommy Jayce\Desktop\Boxing Center\Plannings\box-plus` : référence historique catalogue / logique PayPlug ; aucun nouveau pull ni import demandé pour cette passe ciblée.
- `C:\Users\Mommy Jayce\Desktop\amaz\_` : référence historique de logique et sécurité ; pas de copie aveugle de mécanismes sans besoin constaté.
- `C:\Users\Mommy Jayce\Desktop\skills` : doctrines Baffled Bar, Fusion Baffled, Second Brain et passe critique utilisées pour conserver le niveau d’exigence.

---

## Historique des passes précédentes — domaines et piles potentiellement périmés


Mise à jour : 10 septembre 2026, après-midi. Cette section décrit le chantier actuel ; les journaux datés plus bas constituent l’historique.

### Échelle PC, application installable, SEO et référencement IA, 11 septembre

Vu à 1440 × 900 et 1920 × 1080 : le titre du hero était réglé jusqu’à 153 px et mangeait la colonne. `app/scale.css` ramène le hero à 52–86 px, les titres de pages à 42–68 px, les titres de sections à 30–48 px, les sections à 64 px de marge ; le hero tient dans le premier écran avec son bouton. Entrées animées partout : entête, hero, puis chaque section, carte, ligne de famille et pied de page à l’arrivée dans l’écran (observateur dans `Motion`), respect du mouvement réduit. Application installable : `app/manifest.ts`, icônes `public/icons/*` générées depuis le favicon, `public/sw.js` (statique en cache d’abord, pages réseau d’abord, page `/hors-ligne/`, jamais l’API ni les pages privées), enregistré en production seulement, `theme-color`, carte Twitter, icône Apple. SEO : titres limités à 60 caractères (titre absolu sans suffixe quand le nom est long), descriptions construites à partir des faits pour les fiches minces et bornées à 158 caractères, schémas FAQPage sur les neuf guides et ItemList sur les pages de catégorie, SearchAction sur le site, `robots.txt` autorisant explicitement GPTBot, OAI-SearchBot, ClaudeBot, PerplexityBot, Google-Extended et Applebot-Extended, `llms.txt` réécrit avec les familles, les comptes, les guides, la livraison et l’état du service. Noms : mots vides en minuscules à l’intérieur des noms. Reste : régénérer les 1 088 cartes de partage avec les nouveaux noms ; liste des modèles connus pour la casse.

### Mouvement qui renseigne, 10 septembre nuit

La « nouvelle édition » est supprimée du dépôt (composants, textes, route, cookie). Sur l’édition en place, une feuille `app/motion.css` ajoute un mouvement à chaque état, jamais décoratif : arrivée du hero dans l’ordre de lecture puis pose de la pièce ; révélations en cascade des cartes, des lignes de familles et du sac de séance ; trait qui se dessine sous la famille survolée ; pose de chaque vue de l’inspecteur et soulignement du contrôle actif ; flèche des boutons qui indique la direction ; rebond du compteur du panier à l’ajout (événement `boutique:cart-added`) ; et, entre la carte et la fiche, la photo continue par transition de vue native (`pageswap` pose `view-transition-name: product-hero` sur la photo cliquée, la galerie de la fiche porte le même nom). Le bloc `prefers-reduced-motion` neutralise tout, transitions de vue comprises. Les cartes affichent la famille (ou « Enfant ») à la place du mot « Catalogue ».

### Verdict du propriétaire et finition de l’édition en place, 10 septembre nuit

Le propriétaire a jugé la « nouvelle édition » trop lourde et moins bonne que l’édition en place ; elle est retirée de l’affichage par défaut (l’interrupteur n’apparaît plus ; le code reste dans `components/next/`, joignable seulement par le cookie `edition`). Le site, c’est l’édition en place, à finir. Fini ce soir : tous les textes visibles réécrits pour aller droit au but, sans mot de chantier ni supposition de club (accueil, hero, cartes, catalogue, sac de séance, comparateur, fiche produit, panier, reçu, alertes, contact, pied de page, familles) ; plus aucune taille de texte sous 11 px (étiquettes 12 px, petits textes 13 px, corps 16 px) ; noms des 1 036 modèles importés rendus lisibles à la lecture du catalogue (`humanName` dans `lib/database.ts`, les 19 fiches d’origine intactes) ; sections vides masquées sur les fiches ; modèles proches triés par marque puis discipline ; dates de catégories dans le sitemap ; données du sac de séance réécrites. Contrôles : lint, TypeScript, build, 16 tests API.

### Deux éditions sous un interrupteur, 10 septembre soir

L’édition actuelle reste intacte. Une **nouvelle édition** vit à côté, dans le même dépôt, derrière l’interrupteur fixé en haut de chaque page (cookie `edition`, route `/api/edition`, transition de vue entre documents). Elle couvre l’accueil (deux heros comparables en direct : « La pièce, à la loupe » et « Votre liste »), les pages catégorie (trois questions, phrase-réponse dans l’URL) et les fiches produit (objet en grand, tailles en mots, case « vérifié avec mon entraîneur »). Les guides, services, panier et contact s’affichent dans la nouvelle coquille avec leur contenu actuel. Textes : `lib/next/copy/*.ts`, voix du guide `.research/style-guide-voix.md` (« vous », moins de quinze mots, aucun mot de chantier, aucune supposition de club). Corps 17 px, étiquettes 13 px minimum, cibles 44 px. Contrat de fusion et +1 : `.research/fusion-contract-2026-09-10.md`. Édition par défaut : actuelle, tant que le propriétaire n’a pas tranché.

### Portage Vercel + Supabase, 10 septembre fin d’après-midi

L’application a quitté OpenAI Sites / Cloudflare D1 : **Next.js 16 sur Vercel, Postgres Supabase, lien magique Supabase Auth** pour l’atelier. Détail et procédure dans [DEPLOY-VERCEL.md](DEPLOY-VERCEL.md). Contrôles rejoués après portage sur Postgres local : lint, TypeScript, build de production, audit catalogue, 78 + 19 tests unitaires, 16 tests API, 18 contrôles commerce. Le test `test-payplug-sqlite.py` visait les migrations D1 et n’est plus pertinent. Restent à faire par le propriétaire : projet Vercel importé depuis GitHub avec les variables listées, migrations appliquées sur Supabase, URLs de redirection Supabase Auth, domaine.

### Décisions du propriétaire, 10 septembre après-midi

Panier et paiement simulé demandés par le propriétaire (le brief les excluait). Photos et prix des boutiques sources autorisés ; conserver toutes les photos des produits tarifés. Publier d’abord cette version à 1 055 références, importer le gel à 1 230 ensuite. Crawl Le Coin du Ring repris (un collecteur). E-mails via Inlett, pas Resend. Base de données à migrer vers Supabase, sans wrangler ; périmètre d’hébergement à confirmer. Contrôles rejoués sur cette version : lint, TypeScript, audit catalogue, 78 + 19 + 10 tests, 18 contrôles commerce, 1 088 URL rendues avec métadonnées uniques.

## Version en cours de validation

Le panier public reste en **simulation**. Aucun paiement réel ni expédition n’est activé. Le raccordement PayPlug est préparé et verrouillé ; les variables et étapes restantes figurent dans [PAYPLUG-WIRING.md](PAYPLUG-WIRING.md).

### Réalisé dans le code

- Nouvelle présentation de l’accueil centrée sur l’objet et la préparation de séance : inspection du gant, six sélections explicites boxe/MMA, pièces déjà possédées déduites du sac, comparaison de caractéristiques et passage vers les fiches avec contexte.
- Guides enrichis par des décisions propres à chaque sujet, photographies réelles et sources. Trois familles ajoutées : chaussures de boxe/lutte, équipement d’entraînement et sacs de sport. Pagination serveur à 36 produits, liens par page et rejet des numéros invalides.
- **1 055 références** intégrées : 19 précédentes et 1 036 importées, dont 910 Boxing-Shop et 126 Le Coin du Ring. **3 754 déclinaisons tarifées**. Les 13 articles physiques du dépôt box-plus étaient déjà inclus parmi les 19 ; les abonnements et essais n’ont pas été transformés en articles d’équipement.
- Source box-plus mise à jour par `git pull --ff-only` avant extraction, jusqu’au commit `75aff7597f07592a4f18a21d8b88199803135622`. Aucun secret repris.
- Registre privé de **1 436 relevés** avec provenance, anomalies, état de publication et recherche. Les textes sources bruts restent hors de l’application ; descriptions publiques rédigées à partir des faits. Les prix ou variantes ambigus restent à examiner.
- Photos principales locales WebP aux dimensions contrôlées, galeries source conservées. **1 088 cartes de partage distinctes** (1 055 produits et 33 pages), 1200 × 630, vérifiées sans erreur de texte ou de génération. Les vues secondaires distantes ne sont pas toutes optimisées ou vérifiées localement.
- Panier D1 avec cookie HttpOnly, choix de déclinaison, quantités, calcul serveur, protection contre écrasement concurrent et rotation des sessions expirées. Paiement simulé approuvé/refusé, tentative réutilisable sans double commande, reçu privé imprimable/téléchargeable.
- Envoi du reçu préparé via Resend, statuts honnêtes et protection contre doubles envois. **Fournisseur et domaine expéditeur non configurés ; aucune livraison d’e-mail vérifiée.**
- Formulaire raccordé au contrat Inlett fourni : sauvegarde D1, preuve de travail dans un Worker, déduplication, gestion des réponses incertaines et annulation des tâches à la fermeture. Le statut du relais est visible dans les contacts privés ; aucun envoi réel de test n’a été effectué.
- PayPlug : réglages privés, parcours hébergé de test réservé à l’administrateur, retour serveur, IPN vérifiée par récupération authentifiée du paiement, association stricte aux montants/tentatives, rapprochement manuel, conservation des remboursements à vérifier. Le mode par défaut est simulation ; la clé réelle seule ne peut pas ouvrir les encaissements.
- Migrations D1 0002 à 0005 préparées et appliquées localement. L’hôte devra les appliquer lors de la publication de cette version.
- GitHub relié à `Eddy-etame/boutique-de-boxe` ; `origin` pointe vers GitHub, `sites` vers le dépôt d’hébergement. Le dépôt applicatif se trouve dans `site/`.

### Vérifications effectuées

- Build et lint réussis avant le dernier gel des données ; nouvelle validation finale en cours.
- Contrat du catalogue : 1 055 produits, 3 754 déclinaisons, aucune erreur du validateur utilisé par l’éditeur et le panier.
- API locale existante : 16 contrôles réussis. Panier/commande simulée : 18 contrôles réussis ; contrôle complémentaire de la rotation des sessions et des prix par déclinaison à effectuer après la dernière correction.
- Inlett : 43 tests isolés réussis, zéro TODO. PayPlug : 19 tests de l’adaptateur et 35 tests d’orchestration/interface réussis. SQLite : 10 tests sur les migrations et la requête réelle de règlement/remboursement, réussis. Tous ces tests PayPlug/Inlett utilisent des réponses fictives ; aucun paiement ou e-mail externe.
- Les tests reproductibles se trouvent dans `scripts/`. `test-commerce.mjs --email-disabled` nécessite un serveur local dont le service e-mail est désactivé. `test-payplug-sqlite.py` utilise une base éphémère en mémoire.
- Le contrôle visuel final, l’audit HTTP de toutes les pages et la publication de cette révision restent à terminer ; ne pas assimiler les anciens contrôles hébergés à la validation de cette nouvelle version.

### Ce qui reste ouvert

1. Terminer le contrôle visuel bureau/mobile du nouveau parcours, l’audit SEO rendu et la publication privée. L’hébergement actuellement publié est encore la version antérieure tant que la publication n’est pas confirmée ci-dessous.
2. Terminer la collecte du Coin du Ring : 2 420 URL produits ont été découvertes, mais l’ensemble n’est pas encore importé. Son délai public de collecte est respecté ; les packs à plusieurs groupes d’options doivent conserver leur véritable logique avant publication. Les pages Boxing-Shop (1 060) sont collectées ; certaines exigent encore une revue de contenu ou de variantes. DragonSports répond 403 aux accès publics essayés et n’a pas été contourné. **La demande de reprise de tous les catalogues sources n’est donc pas achevée.**
3. Configurer le fournisseur d’e-mail et un domaine d’expédition, puis vérifier un reçu sur une boîte autorisée. Inlett ne remplace pas un service de reçus détaillés documenté.
4. Configurer puis tester PayPlug seulement lorsque le propriétaire le demande. L’aperçu actuel reste privé (un propriétaire, aucun groupe ou invité externe lors de la vérification) : il faut vérifier l’accès des serveurs PayPlug à l’IPN avant toute recette distante.
5. Finaliser la gestion éditoriale et des médias dans le backoffice, les statuts de traitement/export des demandes et une mesure d’audience adaptée. Les produits, imports, demandes, simulations et réglages de paiement disposent déjà d’interfaces ; cela ne constitue pas encore la gestion de toutes les pages/médias prévue au brief.
6. Domaine `boutique-de-boxe.com`, propriété Search Console et indexation à confirmer par le propriétaire. Les canonicals utilisent l’origine Sites réellement disponible. Aucun trafic, classement, Core Web Vitals terrain ou résultat d’indexation n’est inventé.

Aucun score « Baffled Bar 100 % » ou affirmation « impossible de faire mieux » n’est justifié tant que ces points et la revue visuelle finale restent ouverts.

---

# Passation — Boutique de Boxe

Dossier opérationnel mis à jour le 10 septembre 2026. Voir la section Validation finale pour les derniers contrôles et le déploiement. Les demandes de contact, adresses des inscrits et tokens privés ne doivent pas être copiés dans ce document.

## Périmètre et résultat

Catalogue national Boutique de Boxe édité par **SAS BOXING CENTER**, identité dédiée, **19 produits**, **9 guides**, catégories et services. Prix indicatifs et statuts « Bientôt disponible » ; aucun achat, paiement ou réservation. La remise cible de 15 % reste interne.

**V2 annulée. Le client gère Google Search Console pour l’indexation et le SEO naturel. Aucun Google Analytics activé.**

| Élément            | État établi                                                                                        | Limite ou suite                                                                    |
| ------------------ | -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| Pages et SEO       | 49 URLs, 49 titles uniques, 49 descriptions uniques, 97 liens internes vérifiés                    | Contrôle de l’environnement testé ; indexation Google non déduite.                 |
| Produits           | 19 modèles documentés et descriptions originales enrichies                                         | Ajouter uniquement des faits et images vérifiés.                                   |
| Guides             | 9 articles couvrant les intentions du PDF                                                          | Version entièrement en vouvoiement intégrée dans le catalogue éditorial.           |
| Administration     | Ajout/édition complète, photos, caractéristiques, déclinaisons, texte court SEO, retrait persistés | Enregistrer publie immédiatement la fiche en phase catalogue.                      |
| Sitemap            | Suit le catalogue effectif et ses retraits                                                         | Vérifier domaine et URLs finales après raccordement.                               |
| Contacts / alertes | Persistance, doublons et retrait contrôlés                                                         | Aucun e-mail automatique expédié par l’application.                                |
| Origine configurée | https://boutique-de-boxe.etame-eddy01.chatgpt.site                                                 | Version hébergée privée pour consultation ; le domaine public reste à raccorder.   |
| Domaine client     | https://boutique-de-boxe.com cible connue                                                          | Raccordement DNS/TLS et bascule canonical à vérifier.                              |
| Search Console     | Opération prise en charge par le client                                                            | Remettre sitemap et constats HTTP ; état d’indexation à mesurer.                   |
| Analytics          | Mesure maison avec consentement (onglet Audience de l’atelier) ; aucun Google Analytics            | Ne pas annoncer d’attribution à une requête Google : seules les provenances (domaine) sont connues. |

## Preuves de validation

Le rapport .research/seo-qa-final.json, produit par site/scripts/audit-seo.mjs, contient les 49/49/49/97 résultats ci-dessus. Le script contrôle également les réponses 200 des URLs du sitemap sans changement de chemin, un H1 par page, canonical du même chemin, Product sans faux Offer, BreadcrumbList hors accueil, liens internes, 404, noindex de recherche/administration/désinscription et référence au sitemap dans robots.txt.

L’agent principal confirme **16 tests API passés** via site/scripts/test-api.mjs : refus anonyme et d’en-têtes visiteur/administrateur forgés ; accès du propriétaire local ; origine, consentement et variante ; alerte persistée et doublon évité ; contact ; modification visible publiquement puis restauration ; désinscription ; suppression de contact ; corps trop volumineux ; ajout/édition/retrait de produit et sitemap ; limite de fréquence 429. Le script cible le serveur local et des données synthétiques. Il ne remplace pas le contrôle du compte autorisé et de l’infrastructure réellement déployée.

Les validations réalisées et leurs limites sont détaillées dans la section finale. La navigation au clavier n’a pas fait l’objet d’un parcours exhaustif.

## Matrice finale des mots-clés

La propriété désigne la page principale à renforcer pour l’intention, sans interdire une occurrence naturelle ou un lien ailleurs. Aucun volume ni classement n’est inventé.

| Page propriétaire                  | Intention principale                      | Variantes et rôle                                                                                       |
| ---------------------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| /                                  | boutique boxe                             | Boutique de Boxe, boutique sport de combat France, enseigne nationale ; pas de doublon /boutique-boxe/. |
| /materiel-boxe/                    | matériel boxe                             | Vente matériel boxe, équipement boxe anglaise, matos boxe ; catalogue explicitement précommercial.      |
| /gants-de-boxe/                    | gants boxe                                | Gants de boxe, vente gants de boxe ; sélection des modèles et poids.                                    |
| /gants-mma/                        | gants MMA                                 | Modèles, marques et tailles réelles ; comparaison de catalogue.                                         |
| /materiel-mma/                     | matériel MMA                              | Équipement MMA, protections et tenues selon la séance.                                                  |
| /materiel-sport-de-combat/         | matériel sport de combat                  | Équipement sports de combat, matériel clubs/coachs ; intention équipement.                              |
| /boutique-arts-martiaux/           | boutique arts martiaux                    | Matériel arts martiaux, JJB, kimonos, grappling.                                                        |
| /protections-boxe/                 | protections boxe                          | Protège-dents, protège-tibias pieds, protection sports de combat.                                       |
| /accessoires-boxe/                 | accessoires boxe                          | Bandes, cordes, pattes d’ours et autres accessoires réellement présents.                                |
| /textile-boxe/                     | textile boxe                              | Tenue de boxe, débardeur, ensemble enfant, rashguard.                                                   |
| /sacs-de-frappe/                   | sacs de frappe                            | Sac de frappe boxe, dimensions, suspension et installation.                                             |
| /nouveautes/                       | nouveautés équipement boxe                | Ajouts réels au catalogue, sans fausse disponibilité.                                                   |
| /offres-de-lancement/              | ouverture Boutique de Boxe                | Informations de lancement, aucune remise ou date inventée.                                              |
| /guides/                           | guides d’achat boxe et MMA                | Sommaire des décisions et accès aux articles propriétaires.                                             |
| /guide-des-tailles/                | tailles équipement boxe/MMA               | Hub transversal gants, textiles, chaussures et enfants ; renvoi vers le guide des onces.                |
| /guides/choisir-gants-boxe/        | comment choisir ses gants de boxe         | Usage, ajustement, fermeture, matières et budget ; liens vers catégorie et onces.                       |
| /guides/taille-poids-gants-boxe/   | taille et poids des gants de boxe         | Différences 10/12/14/16 oz ; deux exemples du brief réunis sans tableau universel.                      |
| /guides/choisir-gants-mma/         | comment choisir ses gants MMA             | Construction, taille et format d’exercice.                                                              |
| /guides/debuter-boxe/              | équipement pour débuter la boxe           | Matériel indispensable en boxe anglaise, première séance, prêt du club.                                 |
| /guides/debuter-mma/               | matériel pour commencer le MMA            | Équipement débutant et exigences de salle ; pas de liste obligatoire universelle.                       |
| /guides/choisir-protections/       | quelles protections pour sports de combat | Ajustement, usages et consignes fabricant.                                                              |
| /guides/difference-gants-boxe-mma/ | différence gants boxe et MMA              | Comparaison des usages et renvoi aux deux catégories.                                                   |
| /guides/equipement-enfant/         | matériel de boxe pour enfant              | Premiers cours, déclinaisons réelles, confort et encadrant.                                             |
| /guides/equipement-femme/          | équipement de boxe et combat pour femme   | Morphologie, coupe et besoins ; pas de catalogue dupliqué et recoloré.                                  |
| /produits/{slug}/                  | nom exact du modèle et marque             | Référence, taille/poids/coloris attestés ; une URL par modèle.                                          |
| /contact/, /livraison/, /retours/  | service de la boutique                    | Contact réel, frais indicatifs et phase actuelle.                                                       |
| Pages juridiques                   | éditeur et fonctionnement                 | Faits cohérents, sans bourrage de mots-clés commerciaux.                                                |

Recherche, filtres, administration et désinscription ne sont pas des destinations SEO concurrentes. Les guides mènent vers catégories/fiches pertinentes et les fiches vers les décisions de choix. Les mots généraux « sport de combat » et « arts martiaux » sont contextualisés par l’équipement ; aucune encyclopédie artificielle n’est créée pour les répéter. Les ALT décrivent les images.

## Circuit d’import et de gestion du catalogue

### Préparer une nouvelle référence

1. Conserver la source et sa date/révision. Le lot initial box-plus a été lu après le pull demandé à **e1d7e3d0ba170b526def06b69fff12e246246190**. Les registres product-audit/ et benchmark-products/ conservent références, variantes et provenance des médias.
2. Vérifier modèle, marque, référence, famille, tailles/coloris, photos et caractéristiques. Résoudre les contradictions ou laisser la précision inconnue absente. Aucun stock, ancienne promotion, retrait en club, garantie du revendeur ou engagement fournisseur ne doit être transféré.
3. Rédiger texte court SEO, description, usage et entretien documenté en vouvoiement. Définir un ID et un slug uniques et stables, ainsi qu’un prix indicatif. Le statut reste « Bientôt disponible ».
4. Préparer les médias autorisés, cadrés et optimisés. Vérifier les chemins/URLs acceptés et le modèle représenté. Une image ne prouve pas que chaque variante montrée sera vendue.
5. Dans **/atelier/ → Ajouter une référence au catalogue**, renseigner la fiche et ses caractéristiques, déclinaisons et chemins/URLs d’images. **L’enregistrement publie immédiatement** : finaliser les preuves et le texte avant de valider.
6. Vérifier la nouvelle fiche, sa catégorie, ses métadonnées, ses images, la recherche et sa présence au sitemap. Consigner l’ajout et remettre les changements d’URL au client pour son suivi Search Console.

L’interface assure la gestion complète par référence ; aucun import automatique d’un flux fournisseur ou téléversement de fichiers image n’est présumé. Pour un lot important, préparer un fichier de travail contrôlé puis utiliser le circuit applicatif validé, sans remplacer directement la base ni écraser les modifications en production.

### Modifier ou retirer une référence

L’administration édite les photos, caractéristiques, déclinaisons, texte court SEO, description, usage, entretien et prix. L’ID et le slug existants sont conservés. Les valeurs internes de stock et remise ne deviennent pas une offre publique.

Le catalogue effectif combine le fichier initial site/lib/data/products.json, les entrées persistées de catalog_entries et les modifications de product_overrides. Les ajouts et retraits persistés priment sur le fichier initial ; les modifications de nom/description/prix sont ensuite superposées. Un nouvel import du fichier initial ne doit pas effacer ce travail. Vérifier les informations réellement rendues après toute importation.

Le retrait d’une référence est persistant et modifie le sitemap. Les identifiants et adresses retirés restent réservés ; ils ne peuvent pas être réattribués à un autre modèle. Les alertes conservent le nom de leur référence retirée. Avant de retirer une URL déjà utilisée, traiter ses liens entrants et décider de son devenir ; ne pas prétendre qu’une redirection est créée automatiquement. Pour une nouvelle famille, adapter la taxonomie et les liens du site dans le cycle Sites du projet, sans multiplier les catégories vides.

Contrôles de référence : site/scripts/audit-seo.mjs et site/scripts/test-api.mjs, sur l’environnement prévu par ces scripts. Ils doivent être rejoués après les changements qui affectent leurs fonctions ; le nombre d’URLs attendu évolue avec le catalogue.

### Contacts et alertes

L’administration affiche les 300 demandes les plus récentes. Les compteurs correspondent à cette liste, pas à un total historique illimité. Les contacts se traitent depuis la messagerie habituelle de l’éditeur. Chaque inscription possède un lien individuel de désinscription à inclure dans le message correspondant. Aucun envoi automatique n’est actif.

La suppression retire les données d’une demande ; elle n’est pas un simple marquage « traité ». Ne pas copier les adresses, messages ou tokens dans les documents de passation ou les registres de recherche.

## Domaine, e-mail et mesure : suites externes

**Domaine.** Raccorder boutique-de-boxe.com, vérifier DNS/TLS et réponses HTTP, puis aligner shop.origin, canonicals, sitemap, Open Graph, URLs structurées et liens de retrait. Définir le comportement de l’origine d’hébergement secondaire afin de conserver une origine principale cohérente. Remettre l’URL du sitemap du domaine final au client.

**E-mail.** Les inscriptions sont enregistrées, les désinscriptions fonctionnent, mais l’application n’expédie aucun message automatique. Pour l’activer ultérieurement : raccorder un service et son domaine d’expédition, reprendre l’objet exact du consentement, intégrer le retrait individuel et tester avec une adresse interne autorisée. Une alerte produit ne devient pas une campagne générale.

**Administration en production.** Le 10 septembre, le compte propriétaire connecté à ChatGPT a ouvert /atelier/ et chargé les 19 références et les listes de demandes depuis la base hébergée. ADMIN_EMAIL est configuré comme secret. Aucun identifiant privé n’est conservé dans cette passation.

**Search Console, gérée par le client.** Le client vérifie sa propriété et soumet le sitemap après publication/raccordement. Suivre par date, page et requête : état d’indexation, impressions, clics, CTR et position moyenne. Les clics Google ne sont pas un nombre de sessions toutes sources. Compléter séparément avec les contacts et alertes réellement enregistrés, sans attribution inventée.

| Date/période                     | URL ou requête | Indexation GSC | Impressions | Clics | CTR | Position moyenne | Modification publiée | Contacts/alertes |
| -------------------------------- | -------------- | -------------- | ----------- | ----- | --- | ---------------- | -------------------- | ---------------- |
| Premier relevé réel à renseigner | —              | —              | —           | —     | —   | —                | —                    | —                |

Aucune propriété GSC vérifiée, position, tendance ou collecte GA n’est déclarée dans ce tableau vide. L’onglet « Suivi SEO » de l’administration prépare l’accès aux repères ; il ne constitue pas une connexion automatique à Search Console.

## Avant les ventes réelles

Confirmer fournisseurs et stock, transporteur et frais définitifs, remise justifiable, paiement, commandes, facturation, retours, garanties, médiation et service client. Mettre à jour conditions et données structurées lorsque l’offre devient réelle. Aucun de ces éléments n’est à simuler dans la phase catalogue.

Sources de reprise : decisions.md, open-questions.md, legal-facts.md, seo-evidence.md, seo-qa.json, registres produits et scripts cités. qa-checklist.md reste un instantané historique antérieur ; ses anciennes absences sont remplacées par les preuves plus récentes lorsqu’elles existent.

## Validation finale et direction

- Build de production réussi les 9 et 10 septembre 2026. TypeScript et lint passent. Les images sont servies en WebP 480/960 px ; les polices sont locales.
- Revue dans le navigateur : accueil 1440×1000 et 390×844, fiche et galerie mobile, zoom, index/article de guides, navigation mobile, état vide avec budget/famille, réinitialisation, alerte puis lecture dans l’administration, absence de débordement et d’image cassée sur l’accueil mobile. Le bouton de réduction des animations a été vérifié par lecture du style calculé (`animation: none`).
- WebMCP `filter_catalogue` : inscription du contrat, entrée valide « gants boxe », entrée invalide numérique rejetée, « protège dents » et cohérence de la liste visible vérifiées dans un contexte compatible.
- Les 16 tests API incluent aussi le POST natif du contact sans JavaScript (redirection 303 après persistance). Les données synthétiques ne sont pas transférées dans la base hébergée.
- Lint : composants et hooks non modifiés du catalogue shadcn exclus ; TypeScript continue à vérifier les imports. La recommandation Next/Image est désactivée car les médias sont optimisés localement avec dimensions et srcset. Les rôles ARIA explicites valides sont conservés. La recommandation next/link est aussi désactivée : un défaut de liaison des exports du bundle de production Vinext cassait sa navigation ; les liens HTML natifs assurent maintenant une navigation serveur complète.
- Contrôle de la version hébergée du 10 septembre : navigation réelle accueil → menu mobile → catégorie → fiche produit réussie ; aucune nouvelle erreur ou alerte console sur ce parcours. Menu à 11 destinations, zoom à deux niveaux et fermeture Échap avec retour du focus vérifiés. Le cadrage du zoom a été ajusté pour supprimer la grande marge verticale sur téléphone.
- Dernière revue du 10 septembre : corrections de la synchronisation des éditeurs rapide/complet, de la réservation des identités archivées et des libellés d’alertes après retrait. Le test API du catalogue couvre aussi ces deux cas d’archivage. Le navigateur a vérifié les deux sens d’édition : un brouillon périmé est remplacé après la sauvegarde de l’autre formulaire ; les données originales ont ensuite été restaurées.
- +1 retenu : « Dans votre coin », une sélection par séance dont les familles et la justification changent ensemble. Le cas MMA enfant dit explicitement quand aucune référence vérifiée ne peut être proposée. Aucune prétention d’invention mondiale.
- Références : rythme éditorial, lecture de l’objet et comparaison utile ; banque locale et 10 sources fraîches documentées dans le registre privé. Pas de certification de niveau Sdes références depuis leurs seuls HTML.
- Limites de mesure : pas de test sur appareil Android physique ni de mesure terrain Core Web Vitals ; pas de classement Google ou d’indexation déduit du code. L’envoi d’e-mails et le raccordement du domaine restent des connexions externes.

## Exploitation locale

Le projet exécutable est `site/`. Installer avec `pnpm install`, puis `pnpm dev` ; l’administration locale utilise le compte de test Sites après clic sur le lien de connexion. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build` assurent les contrôles de source. Les scripts `node scripts/test-api.mjs` et `node scripts/audit-seo.mjs` ciblent le serveur local sur 3000 ; ne pas lancer le test de mutation contre la production.

Les migrations sont dans `drizzle/`. Les bases locale et hébergée sont distinctes. Les secrets de production se configurent dans Sites, jamais dans Git. `.openai/hosting.json` conserve uniquement l’identifiant Site et le nom logique DB.


## Passe critique demandée après livraison — 10 septembre 2026

Ce verdict remplace toute lecture de « livrée » comme « Baffled Bar atteinte ». Le catalogue est publié et utilisable ; une refonte ciblée et des fonctions d’exploitation restent à réaliser. Ce tour est un audit, sans modification applicative ni nouveau déploiement.

### Estimation honnête

Les chiffres suivants sont des estimations de pilotage, pas des mesures de qualité automatiques ni des résultats Google : environ **70 % de couverture opérationnelle de la phase 1** (fourchette 65–75 %) ; environ **20 % de la Baffled Bar**. Cette seconde note est un jugement exigeant sur la singularité et la finition : la structure existe, mais la mise en scène, la motion, les détails éditoriaux et la preuve de performance restent insuffisants.

| Chantier | Avancement estimé | Acquis | Écart restant |
|---|---:|---|---|
| Catalogue et parcours de base | 85–90 % | 19 produits, 9 guides, recherche/filtres, fiches, alertes et contact persistés | Assortiment limité, curation par séance et relations entre produits trop mécaniques |
| Préparation SEO technique | 80–85 % | URLs, titres/descriptions uniques, canonical, sitemap, schémas, maillage | OG incomplet, URL image structurée externe erronée, fraîcheur des hubs, tests à élargir |
| Utilité et singularité éditoriales | Environ 70 % | Informations produit sourcées en registre et textes originaux | Répétitions, peu de preuves visibles, guides trop textuels, critères de choix insuffisamment illustrés |
| Backoffice permettant de tout piloter | 55–65 % | Éditeur produits et boîte de demandes | Pages/guides/catégories, médiathèque, brouillons, traitement des demandes, tableaux réels |
| Analytics réellement raccordés | 0 % | Choix Search Console confié au client ; structure de relevé dans la passation | Collecte de visites/événements, tableau et import/connexion GSC absents |
| Domaine public/indexation | Non achevé | Aperçu privé opérationnel | Raccordement et ouverture publique, puis indexation et mesure réelle |

La note du backoffice concerne le nouveau sens « tout gérer », plus large que la seule ligne produits/prix/stock du PDF. Les paiements, commandes, factures, transporteurs et stocks fournisseurs sont explicitement reportés à la phase 2 (PDF p.13). La V2 comparative reste annulée.

### Constats par regard critique

- **Hater / originalité.** Le grand titre condensé noir/bleu, les rectangles gris, les numéros décoratifs et les flèches se répètent. Le résultat reste compatible avec un modèle de boutique premium pour beaucoup d’autres produits. Des phrases comme « Quelques grammes. De vraies questions. » et « La précision commence ici. » sonnent trop fabriquées. Les photos sont réelles ; le problème est la composition et la cadence éditoriale, pas une prétendue détection d’images IA.
- **Direction artistique.** Accueil parcouru intégralement sur bureau 1440×1000 : hiérarchie lisible, mais peu de changements d’échelle ou de mise en scène après le hero. Les trois contrôles du hero changent une photo/un texte ; aucune inspection reliée visuellement au détail évoqué. Sources : components/home.tsx:22–180 ; components/shop-interactions.tsx:81–148.
- **Motion.** Le mouvement principal est un fondu/déplacement vertical de 22 px en 0,75 s et un fondu d’arrivée ; quelques survols complètent le tout. Il manque une orchestration qui aide à inspecter, choisir et comparer le matériel. Sources : app/shop.css:1382–1405 ; components/shop-shell.tsx:180–199. La navigation serveur fonctionne, mais aucune transition visuelle de page spécifique n’est construite.
- **Visiteur mobile.** Le guide /guides/debuter-mma/ a été inspecté sur téléphone simulé : pas de débordement horizontal (largeur de contenu et document égales à 375 px dans ce panneau). Les longs blocs restent lisibles mais manquent d’illustrations et de résumés utiles. Sur l’accueil, les résultats du choix de séance viennent longtemps après les commandes ; le changement de sélection n’est pas immédiatement visible à l’écran.
- **Pratiquant / coach.** « Dans votre coin » est un point de départ utile, mais la sélection prend le premier produit dans chaque famille admissible. Le parcours MMA première séance conserve le débardeur Training : sa pertinence n’est pas établie par une règle de compatibilité détaillée. Le mécanisme n’atteint pas encore le +1 revendiqué. Sources : components/shop-interactions.tsx:351–386. Les guides n’affichent pas les liens de preuve présents dans les données et n’ont pas de relecture experte attestée.
- **Google et aperçus de partage.** Les fondamentaux sont en place, mais les dix catégories réutilisent exactement leur introduction dans « Comment choisir ? » (app/[...slug]/page.tsx:353,363). Les catégories/guides n’émettent pas d’image OG dans les réponses vérifiées localement (même fichier:88–106). Une URL image HTTPS acceptée dans l’admin devient malformée dans Product JSON-LD à cause de shop.origin + i.src (:306). Les dates des URLs hors produits restent fixées au 9 septembre (app/sitemap.ts:8–25). Le mot « sous-gants » apparaît dans la description de la catégorie accessoires sans référence autonome correspondante (lib/catalog.ts:112).
- **Gérant / exploitation.** Il existe un éditeur de références et une boîte de réception, pas un CMS complet. Pas de téléversement d’image, de statut brouillon, de gestion éditoriale, de traitement/recherche/export des demandes, d’envoi et d’historique d’e-mails. L’onglet SEO explique la mesure mais n’affiche aucune collecte réelle. De plus, une erreur de base renvoie silencieusement le catalogue initial et peut réafficher des produits retirés (lib/database.ts:62–64) : il faut une politique de dégradation explicite.

### Preuves et limites de ce tour

Relecture complète des 15 pages du brief par le second auditeur ; audit du code SEO et des réponses locales de catégories/guides par un auditeur indépendant ; accueil parcouru de haut en bas dans le navigateur hébergé ; guide débuter MMA choisi hors du dernier chantier visuel et parcouru ; contrôle mobile ; choix MMA réellement cliqué. Aucun nouvel avertissement ou erreur console observé pendant ce parcours. Les 16 tests API et le build restent les preuves du tour de livraison, pas des tests prétendument rejoués ici. Aucun classement, trafic, vitesse 4G, test sur appareil peu puissant ou Core Web Vitals terrain n’est déduit de ces observations.

Corrections déjà livrées au tour précédent : navigation native après défaut Vinext, synchronisation des deux éditeurs, réservation des identités archivées, libellés d’alertes retirées, cadrage du zoom. Les nouveaux constats ci-dessus restent ouverts ; ils ne sont pas présentés comme corrigés.

### Direction recommandée pour dépasser le modèle actuel

**Le matériel préparé pour la séance.** Garder la rigueur du catalogue, reconstruire l’expérience autour des objets réels et du choix : présentation de pièces avec échelle et texture, annotations attachées à une fermeture ou une zone montrée, comparaisons de caractéristiques vérifiées, sélections explicitement éditées par contexte, repères de choix dessinés dans les guides. Chaque sous-page doit apporter une décision propre plutôt qu’un bloc introductif répété.

Motion : transitions brèves et tendues pour sélectionner ; déplacements qui relient la commande au résultat ; inspection plus lente pour lire une matière ; progression au scroll réservée aux moments explicatifs. À tester avec mouvement réduit et sur téléphone. Ajouter des effets seuls ne corrige ni la curation ni le texte.

Référence supplémentaire observée ce tour : https://teenage.engineering/products/ep-133 — narration et vue matérielle de l’objet, inspectées dans le navigateur et dans la page source le 10 septembre. Les dimensions du panneau de cette référence étaient celles de bureau ; aucune validation mobile/performance ni certification S ne lui est attribuée. On explore cette couche d’explication du produit ; ses photos, sa campagne Muhammad Ali et son apparence ne sont pas à reprendre. L’URL On /explore a redirigé vers /shop dans la lecture Web ; elle n’est pas retenue comme nouvelle preuve de motion.

Ordre recommandé : corriger les défauts de vérité/SEO/stock dégradé ; renforcer la direction artistique, la curation et les interactions de choix ; valider la nouvelle expérience ; puis compléter le backoffice et raccorder la mesure. La condition du client (« si plus aucune amélioration significative ») n’est pas atteinte aujourd’hui.

### Backoffice et analytics de l’étape suivante

Un seul espace de travail : produits/déclinaisons, médias, catégories, guides/pages, demandes/alertes, référencement et tableau de mesure. Prévoir brouillon/aperçu/publication, récupération des retraits, statuts de traitement, recherche/export, envois avec consentement et historique. Les fonctionnalités commerciales de phase 2 restent séparées.

Mesurer les vues de fiches, usages de recherche/filtres, passages guide→produit, alertes réellement enregistrées et contacts réellement enregistrés. Distinguer les visites du site des impressions/clics/requêtes Search Console. Le choix d’un fournisseur, ses accès et les éventuelles règles de consentement devront être résolus à ce moment ; aucun service n’est déclaré raccordé dans ce tour.

## 11 septembre 2026 — base Supabase, page de santé, variables

- Le schéma (8 tables, sécurité par ligne activée) est livré en un fichier à coller : `drizzle/supabase-init.sql`, idempotent. La migration `drizzle/0001_rls.sql` porte la même sécurité pour `pnpm db:migrate`. Le propriétaire l’a exécuté dans l’éditeur SQL de Supabase le 11 septembre.
- `/api/health` répond sans secret : type de connexion (`pooler`, `direct`, `local`), port, nombre de tables, catégorie de panne (`no-url`, `bad-url`, `auth`, `dns`, `unreachable`, `no-tables`). Première lecture en production : `DATABASE_URL` pointait sur la base locale (`127.0.0.1:54329`), d’où les 503 du panier et des alertes en 0,2 s. Correction : importer `.env.vercel` (chaîne du pooler, port 6543) puis redéployer.
- Les routes API journalisent désormais le code d’erreur de la base (`commerce GET/POST`, `api POST`) dans les journaux Vercel.
- Un seul fichier de variables pour Vercel : `.env.vercel` (ignoré par Git). `.env.local` reste le poste local ; `.env.example` est la référence sans valeur ; l’ancien `.env` de l’ère Cloudflare est supprimé.
- Fichiers publics : `Cache-Control` un an immuable pour `/fonts` et `/icons`, trente jours avec revalidation pour `/products` et `/media` (Vercel servait `max-age=0`).
- Temps de réponse en production après le disjoncteur et la mémoïsation du catalogue : accueil 1,4 s à froid, 0,4 s à chaud ; catégories 0,4 à 0,9 s.
- Serveur MCP Supabase déclaré dans `.mcp.json` à la racine du dossier de travail (hors dépôt) ; l’authentification se fait dans un terminal interactif (`claude /mcp`).
- 11 septembre, 10 h 40 : `DATABASE_URL` corrigée sur Vercel (pooler eu-west-2, port 6543). `/api/health` répond `ok, pooler, 8 tables`. Tests en production : panier ajout/lecture/vidage 200 (2,6 s à froid, 0,45 s à chaud), alerte d’ouverture 201, demande de contact 201 (enregistrement seul, le relais Inlett part du navigateur), lien magique 200 pour une adresse inconnue et pour le propriétaire. Pages avec base : accueil 1,1 s, catégorie 0,44 s, fiche 0,36 s, atelier 0,32 s. Deux lignes de test à supprimer dans l’atelier : alerte `test-boutique-2026-09-11@example.com` (launch) et demande `f52dbbe8-b88e-41fe-88ae-a067238d9dbc`.

## 11 septembre 2026, après-midi — noms, guides, vignettes, fiche

- **Noms du catalogue importé (1 036 fiches).** `scripts/name-products.mjs` applique les règles de `.research/regles-noms-produits.md` : forme « Objet Marque Modèle, variante », marque une seule fois, codes fournisseur sortis du nom (ligne Référence ou Référence interne), couleurs dans `colors`, mentions entre parenthèses dans `notes`, tailles normalisées (S/M/L, « 10 oz », couleur retirée quand elle est commune à toutes les tailles), 14 collisions résolues (référence ou taille réintroduite), 7 marques adoptées depuis le nom, 13 fiches à relire (liste dans `.research/names-report.md`). Le nom fournisseur reste dans `sourceName`, les étiquettes d’origine dans `sourceSizes` ; le script est idempotent et repart toujours de ces champs. `humanName` a disparu de `lib/database.ts` : les noms sont définitifs dans les données. Audit, 78 tests unitaires, 16 tests API et 18 tests commerce passent.
- **Guides.** Neuf guides relus contre le guide de voix : phrases de 22 mots au plus, « votre salle » et « votre entraîneur », plus de « référence », « pièce » ni de marque concurrente citée ; surtitres factuels (« Guide d’achat · Gants de boxe »).
- **Vignettes sociales.** 1 088 cartes régénérées avec les nouveaux noms ; surtitre « MARQUE · FAMILLE », faits « MATIÈRES / TAILLES / COULEURS », pied « LIVRAISON DANS TOUTE LA FRANCE » (plus de « PAR BOXING CENTER » : la boutique est une identité distincte). Générateur : `.research/generate-og.py --with-products --workers 8` puis `.research/copy-final-assets.py`. Poids du dossier : 115 Mo, ~110 Mo ajoutés à l’historique Git à chaque régénération ; prochaine étape recommandée : cartes générées à la demande (`ImageResponse`) pour sortir les PNG du dépôt.
- **Fiche produit.** Référence fabricant et couleurs dans les caractéristiques, famille lisible en surtitre, filtre « Taille » limité à la taille seule ; sur l’accueil, l’inspecteur agrandit vraiment la photo quand le modèle n’en a qu’une.

## 11 septembre 2026, soir — référencement moteurs et IA

- **Un graphe JSON-LD par page** (`lib/seo.ts`) : Organization + OnlineStore et WebSite dans le gabarit ; ItemPage + Product (sku, mpn, marque, couleur, tailles, matière, famille, photos, prix prévu, modèles proches) + ProductGroup sur les fiches ; CollectionPage + ItemList sur les familles ; WebPage + Article + FAQPage sur les guides ; WebPage + liste des familles sur l’accueil ; BreadcrumbList porté par le composant avec un `@id`. Entités ancrées sur Wikidata (14 identifiants vérifiés sur l’API le 11 septembre). Pas d’`offers` avant l’ouverture des ventes.
- **Mots-clés** : `KEYWORDS` par famille (tête et corps), `PAGE_KEYWORDS` par page de service, `productKeywords` par fiche (objet, marque, tailles en oz, couleurs), guides. Publiés en `<meta name="keywords">` et dans les nœuds WebPage/Article ; l’audit vérifie qu’un mot-clé de tête existe dans le texte visible. Métadonnées `creator` = Eddy Etame Etame, `publisher` = SAS BOXING CENTER.
- **Fichiers et interfaces agents** : `/llms.txt` et `/llms-full.txt` générés depuis les données (244 Ko, tous les modèles et guides), `/catalogue.json` (flux, CORS), `/api/mcp` serveur MCP en lecture seule à huit outils, `/.well-known/mcp.json`, `/ai.txt` (règles pour les IA), `/humans.txt` (paternité : Eddy Etame Etame), `/.well-known/security.txt`, clé IndexNow publiée et `scripts/indexnow.mjs`. `robots.txt` autorise explicitement `/api/mcp`. Sitemap avec images des fiches, fréquence et priorité.
- **Marques** : forme d’affichage unique dans les données (`Fairtex`, `Elion`, `Cleto Reyes`…), `sourceBrand` conservé ; les noms de fournisseurs ne sont plus des marques.
- Doctrine et passage au domaine final : `docs/SEO-GEO.md`. Audit étendu : `scripts/audit-seo.mjs` (graphes, résolution des `@id`, mots-clés, fichiers agents, MCP).

## 11 septembre 2026, soir — les requêtes du cahier des charges, page par page

- **Registre des requêtes** (`lib/seo-copy.ts`) : les 18 requêtes prioritaires du cahier des charges ont chacune une page canonique (`QUERY_MAP`). Chaque page de famille ou de hub porte un titre qui commence par la requête exacte, un surtitre en capitales avec la forme courte (« GANTS BOXE », « MATÉRIEL BOXE », « BOUTIQUE ARTS MARTIAUX »), un texte de fond de trois à quatre sections H2 et trois questions visibles publiées en FAQPage. L’audit vérifie que les requêtes prioritaires existent dans le texte visible.
- **Deux pages ajoutées** : `/boutique-boxe/` (hub « boutique boxe », tout le catalogue) et `/arts-martiaux/` (famille jusque-là sans page).
- **24 sous-familles** (`lib/subfamilies.ts`) : bandes, sous-gants, protège-dents, casques, protège-tibias, coquilles, pattes d’ours, cordes à sauter, poires de vitesse, sacs sur pied, rashguards, shorts MMA, shorts de boxe, t-shirts, sweats, débardeurs, leggings, kimonos, ceintures, gants enfant, gants à lacets, gants sparring, matériel enfant, matériel femme. Chaque page : requête, texte propre, liste filtrée par règle, questions, lien vers la famille et le guide. Vignette sociale empruntée à la famille parente.
- **Fiches produit** : description longue générée depuis les faits (`lib/describe.ts`, tournures variées par identifiant, jamais de stock ni de remise), discipline, niveau de pratique conseillé, famille, référence fabricant, conseils d’entretien par famille, lien vers la sous-famille.
- **Trois guides ajoutés** (12 au total) : « Boxe anglaise : le matériel indispensable », « MMA : l’équipement obligatoire pour débuter », « Quelle taille de gants de boxe choisir ? ».
- **Ajout rapide sur les cartes** : un signe plus ; sans taille, le modèle part au panier ; avec tailles, le choix s’ouvre sur la carte.
- **Catalogue** : 13 fiches à relire retirées de la publication (`.research/produits-a-relire.json`), 1 042 modèles publiés.
- Google Search Console : balise de vérification par variable `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION`.

## 11 septembre 2026, fin de journée — vignettes à la demande, mesure, portée nationale

- **Vignettes sociales à la demande** : `app/vignette/[kind]/[clé].png` (`lib/og.ts`) rend chaque carte avec les polices du site (Barlow Condensed, Manrope, instanciées en TTF dans `assets/fonts`) et la photo du modèle convertie en PNG à la volée (`sharp`, importé hors bundle). 1 088 PNG et `og.json` retirés du dépôt. Toute page, y compris les sous-familles et les guides ajoutés, a sa carte. Cache d’un jour côté client, sept jours côté CDN.
- **Mesure d’audience maison** (décision du propriétaire : pas de Vercel Analytics, pas de Google, tout dans l’atelier). `components/consent-tracker.tsx` affiche une carte de consentement à la première visite (« J’accepte » grand et vert, « Refuser » discret ; lien « Cookies » du pied de page pour changer d’avis). Sans accord, rien n’est posé ni envoyé. Avec accord : cookie `bdb_consent` (un an), cookie d’identifiant `bdb_vid` (treize mois), session de trente minutes en `sessionStorage`. Événements envoyés à `POST /api/analytics` par lots (beacon à la sortie) : page vue (largeur, langue, première visite, provenance), sortie (temps passé, profondeur de lecture), clic (libellé, lien, zone), ajout au panier (carte ou fiche), alerte, contact, recherche (texte et nombre de résultats), filtre, consentement. Le serveur ne garde que les clés connues, borne les nombres, coupe les textes, refuse les chemins `/atelier`, `/connexion`, `/api`, limite à 600 événements par heure et par adresse (adresse hachée, jamais stockée) et crée la table `events` au premier événement si elle manque (`drizzle/0002_events.sql` pour Supabase). Onglet **Audience** de l’atelier (`components/audience.tsx`, `GET /api/analytics?days=7|30|90`) : pages vues, visites, visiteurs, temps moyen, visites d’une seule page ; modèles vus jamais ajoutés au panier, pages où les visites s’arrêtent, recherches sans résultat ; pages les plus vues avec temps et lecture, pages d’entrée et de sortie, enchaînements les plus fréquents, modèles les plus vus avec ajouts, actions, recherches, appareils, provenances, par jour. Tests : 4 contrôles ajoutés à `scripts/test-api.mjs` (20 au total). Vérifications Google et Bing par variables `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` et `NEXT_PUBLIC_BING_SITE_VERIFICATION`.
- **Fiche produit, passe de mouvement et d’usage** (`components/shop-interactions.tsx`, `app/motion.css`, `app/shop.css`) : la colonne de lecture arrive dans l’ordre à côté de la photo ; sous la souris, la photo s’agrandit autour du point regardé (loupe, `--ox/--oy`, souris seulement, le plein écran reste au doigt) ; flèches gauche/droite entre les vignettes ; la taille choisie et la vignette survolée se sentent ; faits et caractéristiques se posent ligne à ligne ; la confirmation d’ajout arrive sans saut. Sur téléphone (≤ 800 px), une barre d’achat fixe en bas suit le lecteur dès que le bouton principal sort de l’écran : prix de la taille choisie, « Choisir la taille » (fait défiler jusqu’aux tailles) ou « Ajouter au panier » (déclenche le bouton principal), « Ajouté » pendant 2,6 s ; elle reste rentrée tant que la carte de consentement est ouverte (`html[data-consent-open]`). Tout est neutralisé par `prefers-reduced-motion`.
- **Pages de catalogue, passe de mouvement** (`app/motion.css`, `components/shop-interactions.tsx`, `components/shop-shell.tsx`) : le titre s’écrit, l’accroche et le texte suivent ; les sous-familles arrivent une à une ; la grille se recompose à chaque recherche, filtre ou page (clé de la grille = signature des filtres, cartes en cascade, compte qui se réécrit) ; le panneau de filtres et ses champs arrivent ; critères, familles voisines, texte de fond et questions se posent quand leur section arrive ; la réponse d’une question arrive à l’ouverture. Les cartes du catalogue ne passent plus par la révélation générale de la coquille (elles ont leur cascade propre). Neutralisé par `prefers-reduced-motion`.
- **Carte des cookies bloquante** (`components/consent-tracker.tsx`, `app/layout.tsx`) : elle couvre l’écran dès le premier rendu (script en tête qui pose `html[data-consent-open]` sans choix enregistré), la page derrière est floutée et ne défile pas, le focus reste dans la carte, « J’accepte et j’entre » en vert sur toute la largeur, « Continuer sans mesure » discret. Au choix, `boutique:replay-entry` fait rejouer l’entrée (animations d’arrivée, sections révélées visibles, compteurs). Vercel Web Analytics (`@vercel/analytics`, sans cookie) posé dans le gabarit en complément : à activer dans le projet Vercel ; la politique de confidentialité le mentionne.
- **Audience, parcours** : `GET /api/analytics` renvoie `journeys` (40 dernières visites ayant au moins une page : appareil, provenance, pages dans l’ordre avec temps passé, ajouts, recherches, alertes, contacts) ; bloc « Parcours des dernières visites » dans l’onglet Audience.
- **Clients & ventes** (`lib/clients.ts`, `components/clients.tsx`, onglet de l’atelier) : clients par e-mail (nom, téléphone, accord e-mail, commandes approuvées/refusées, total, première/dernière, modèles), chiffre d’essai, panier moyen, fidèles, ventes par jour, par modèle, par famille, par marque, livraison choisie, entonnoir fiches vues → ajouts → commandes ; exports CSV `GET /api/commerce/admin-export?kind=clients|ventes` (UTF-8 BOM, point-virgule). Le tunnel d’essai capture un téléphone facultatif et un accord e-mail (case non cochée par défaut) : colonnes `phone` et `optin` sur `simulation_orders` (migration `0003_buyer_contact.sql`, ajoutées d’elles-mêmes au premier passage si elles manquent). Tests : 23 contrôles dans `scripts/test-api.mjs`.
- **PayPlug** : voir `docs/PAYPLUG-WIRING.md` (état du 11 septembre tard) : défaut Postgres corrigé, test bac à sable exécuté jusqu’à la page de paiement, attribution et notifications documentées.
- **Adresse de livraison à la caisse** (réponse du propriétaire, 11 septembre tard) : adresse, complément, code postal à cinq chiffres et ville sont demandés avec le nom, l’e-mail et le téléphone ; colonnes `address1`, `address2`, `postcode`, `city` sur `simulation_orders` (migration `0004_delivery_address.sql`, ajoutées d’elles-mêmes au premier passage). L’adresse de facturation PayPlug sert d’adresse pour les paiements de test. Les exports CSV clients et ventes portent téléphone, adresse et mode de livraison : prêts pour l’expédition. Les essais refusés restent dans la liste des clients, comptés à part. Suites : `test-api.mjs` 23, `test-commerce.mjs --email-disabled` 18, `test-payplug` 19, `test-payplug-orders` 35.
- **Mots-clés du brief, vérifiés à chaque audit** : `scripts/audit-seo.mjs` lit la carte des requêtes (`get_query_map`) et exige, pour chacune des 20 requêtes (les 18 du brief plus sac de frappe et chaussures), la phrase exacte dans le titre, le H1 ou l’accroche, la description, le texte visible (au moins deux fois), le graphe JSON-LD, et un lien depuis l’accueil. Titres, descriptions, accroches et questions fréquentes des hubs et familles ont été réécrits pour porter les phrases telles quelles (« vente matériel boxe », « matos boxe », « équipement boxe anglaise », « boutique sport de combat France », « vente gants de boxe », « gants boxe »…). Le pied de page porte sur chaque page une ligne « Recherches fréquentes » avec les 20 requêtes en toutes lettres vers leur page.
- **Vers 100 % partout, dernière passe du 11 septembre** : le voile de la carte des cookies est rendu dès le serveur (flou `backdrop-filter` sur le voile, plus de filtre sur toute la page ; caché par le CSS quand le choix est déjà fait, donc aucun éclair ni pour les uns ni pour les autres) ; titres des trois hubs ramenés sous soixante caractères avec la requête en tête ; **tableau des mots-clés du brief dans l’onglet Suivi SEO** (`lib/brief.ts`, `GET /api/seo/brief`) : 20 requêtes sur 20 entièrement portées dans les textes propres du site (titre, accroche, description, deux occurrences dans le corps, réponse citable qui commence par la requête) ; réponses citables réécrites pour commencer par la requête exacte ; Audience compare chaque total à la période précédente ; Clients & ventes : bouton « Effacer » (droit à l’effacement : nom, e-mail, téléphone, adresse effacés sur commandes, paiements et alertes, la commande reste pour les comptes) et historique filtré par la recherche. Suites : API 24, commerce 18, PayPlug 19 et 35, audit 1 104 pages avec la passe du brief.

### Prochaine étape : l’ouverture des ventes, dans l’ordre

1. **Domaine** : attacher `boutique-de-boxe.com` et `www` au projet Vercel et pointer les DNS chez le registrar ; canonicals, sitemap, hreflang, graphe et `llms.txt` sont déjà fixés sur ce domaine dans le code (aucune variable, aucune redirection).
2. **Moteurs** : vérifier le domaine dans Search Console et Bing Webmaster (`NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION`, `NEXT_PUBLIC_BING_SITE_VERIFICATION`), soumettre `/sitemap.xml`, relancer `node scripts/indexnow.mjs` ; faire pointer les six sites des salles vers la boutique avec les phrases du brief en texte de lien ; renseigner `SAME_AS`.
3. **Mesure** : activer Analytics dans le projet Vercel ; l’onglet Audience tourne déjà.
4. **PayPlug** : finir le test de carte (Atelier → Réglages PayPlug), puis le tour complet en ligne ; ouvrir le compte marchand propre à la boutique et remplacer les clés ; retirer le verrou `LIVE_PAYMENT_RELEASED` dans une modification revue, jamais par un simple collage de clé.
5. **Catalogue vendable** : stocks par déclinaison et réservation, prix définitifs et TVA, frais de port par transporteur et point relais, matériel lourd.
6. **Après-vente** : e-mail transactionnel (Resend et domaine expéditeur), factures numérotées à la place des reçus d’essai, CGV définitives et droit de rétractation, procédure de remboursement.
7. **Bascule** : `COMMERCE_MODE` en mode réel, tunnel public branché sur PayPlug, recette complète avec cartes réelles à petit montant, puis ouverture et campagne d’alertes (les inscrits de « Ouverture des ventes » attendent).

- **Portée nationale et moteurs de réponse** : `hreflang` fr-FR + x-default sur chaque page, noms alternatifs « Boxing Boutique » dans le graphe, réponses citables d’une phrase par requête (`QUERY_MAP.answer`) publiées dans `/llms.txt`, `/llms-full.txt` et l’outil MCP `get_query_map`, dix robots d’IA supplémentaires autorisés, redirection 301 de l’alias vercel.app vers le domaine dès que `NEXT_PUBLIC_SITE_ORIGIN` le porte.

## 11 septembre 2026, soirée — accueil : texte d’ouverture et mouvement

- **Texte d’ouverture** composé par le propriétaire à partir de trois propositions : « Tout le matériel de boxe et de MMA : gants, bandes, protections, sacs de frappe. / Le matériel des salles, livré chez vous, dans toute la France. / Ouverture des ventes bientôt. Laissez votre e-mail, on vous prévient le jour J. » Le lien mène à la section d’alerte (`#ouverture`). Le bouton du hero mène à `/boutique-boxe/`.
- **Mouvement** (`components/shop-shell.tsx`, `app/motion.css`) : les comptes de familles montent de 0 à leur valeur quand la ligne apparaît (900 ms, chiffres à largeur fixe) ; la photo du hero prend du relief sous le pointeur (rotation de 3 à 4 degrés, souris seulement) ; les titres de section s’écrivent de gauche à droite ; sur les cartes, la photo avance, la flèche part et le nom se souligne ; sur la fiche, chaque photo choisie se pose. Tout est neutralisé par `prefers-reduced-motion` et par le bouton « Réduire les animations ». L’entrée du hero utilise `animation-fill-mode: backwards` pour rendre la main à la transformation du pointeur.
- Vérification : le volet navigateur de la session était masqué (animations et compteurs figés par le navigateur) ; le mouvement est à contrôler à l’œil sur un onglet visible.


## 17 septembre 2026 — reçu PDF, vitesse, limites et sécurité

- **Reçu téléchargeable en PDF** (`lib/receipt-pdf.ts`, `pdf-lib`). Le bouton « Télécharger le reçu » sert désormais un vrai PDF (`application/pdf`, texte net et sélectionnable), fidèle au reçu HTML : masthead, bandeau jaune, titre, tableau, total, mentions légales. Rendu vectoriel sans moteur de navigateur (léger et rapide sur Vercel). Sanitisation WinAnsi (l’espace fine insécable de `money()` et les caractères hors Latin-1 sont ramenés à un jeu sûr). L’e-mail du reçu garde son corps HTML. `scripts/test-commerce.mjs` vérifie l’en-tête PDF.
- **Vitesse.** `readCatalog` mis en mémoire par instance sur `globalThis` (TTL 60 s, vidé aussitôt par `bustCatalog()` aux trois écritures de l’atelier) — le catalogue n’est plus relu en base à chaque page, fiche, sous-famille, plan de site, `catalogue.json`, `llms(.full).txt` ; il dépasse la limite de 2 Mo du cache de données de Next, d’où le mémo maison. Recherche d’override en `Map` indexée. `resolveCart` charge panier et catalogue en parallèle ; l’ajout au panier ne relit plus la base (`buildCart`) ; l’envoi du reçu part après la réponse (`after()`). La mesure d’audience insère ses événements en une seule requête (au lieu de 25) et lit son tableau de bord en une seule vague. Première rangée de vignettes non différée. Panneaux de l’atelier chargés à l’ouverture de leur onglet (`next/dynamic`).
- **Limites et sécurité.** En-têtes de sécurité sur toutes les pages HTML via `next.config.ts` (CSP adaptée à Next 16 : `'unsafe-inline'` requis par le runtime et les JSON-LD, `connect-src` pour inlett et Vercel Analytics, `'unsafe-eval'`+ws en dev seulement, `upgrade-insecure-requests` en prod ; exclut `/api` qui garde ses propres en-têtes) ; HSTS, X-Frame-Options DENY, Referrer-Policy, X-Content-Type-Options, Permissions-Policy. Sonde `/api/health` mémoïsée 10 s (plus une connexion par requête). Limite d’abus sur `/api/mcp` (300/h, fail-open) ; `catalogue.json`/`llms` mémoïsés ; `GET /api/catalog` mis en cache CDN. Corps de `/api/analytics` borné (32 ko). Export CSV protégé contre l’injection de formule. `clientIp` ne fait plus confiance à `cf-connecting-ip` avant `x-forwarded-for` (usurpable). Message d’erreur d’audience générique.
- **Domaine, corrigé le 17 septembre.** Le commit `26aa80e` du 16 avait fixé `shop.origin` sur `.fr` et fait rediriger l’alias Vercel vers ce domaine, qui pointe chez OVH : les URL publiques tombaient sur une page vide. Correction : origine fixée sur `https://boutique-de-boxe.com`, redirection supprimée (l’alias sert à nouveau le site), `.fr` retiré partout (code, scripts, `.env`, `security.txt`, docs). Reste à faire côté hébergement : ajouter `boutique-de-boxe.com` et `www` dans Vercel et pointer les DNS.
- **Audit lourd SEO / GEO / mots-clés / métadonnées du 17 septembre (à la main, sans agents).** Matrice par requête du brief, sur la page servie : titre, `og:title`, description, `og:description`, meta `keywords`, H1 ou accroche, présence dans les 600 premiers caractères, deux occurrences dans le corps, graphe JSON-LD, réponse citable, lien en pied de page, lien depuis l’accueil, canonical, `og:url`, `og:image` (vignette), hreflang et origines du graphe sur `boutique-de-boxe.com`, page indexable, carte Twitter : **20 requêtes sur 20 sans manque**. Titres de 49 à 58 caractères, descriptions de 150 à 157. Les six réponses citables qui contenaient leur requête sans l’ouvrir (« Matos boxe », « Matériel MMA », « Sport de combat », « Boutique arts martiaux », « Gants MMA », « Sac de frappe ») commencent maintenant par la requête exacte : les moteurs de réponse citent la première phrase. Maillage : chaque page de requête reçoit de 33 à 66 liens depuis les seize pages pivots, avec la phrase exacte en texte de lien. Fichiers agents alignés sur le domaine : `mcp.json` déclare un `endpoint`, une `url` et des liens `related` absolus ; `ai.txt` porte `Site:` et `Canonical:` ; `humans.txt` porte l’adresse du site. Accueil, fiches, guides, sous-familles, 1 104 URL du plan de site, robots (`Host`, `Sitemap`), `llms.txt`, `catalogue.json`, `security.txt` : tout sur `.com`, aucune trace de `.fr` ni de l’alias. En-têtes lus par les robots : pas de `X-Robots-Tag` sur les pages publiques, `noindex` sur l’API, `application/xml` pour le plan de site. Contrôles : `audit-seo` 1 104 pages, `test-public-shell` (aucune redirection, canonical `.com`), API 24, commerce 18, PayPlug 19 et 35, lint, TypeScript.
- **Questions fréquentes partout (17 septembre, soir).** Le propriétaire a relevé l’absence de FAQ sur une partie du site. Désormais : page `/faq/` (douze questions : ouverture, prix prévus, commande d’essai, livraison, tailles, retours, paiement, marques, enfants, clubs, données, contact), en pied de page, dans le plan de site, `llms.txt` et l’index MCP, avec sa vignette sociale ; accueil : six questions (`HOME_FAQ` dans `lib/seo-copy.ts`) ; livraison et retours : quatre questions chacune (`faq` sur le type `Service`) ; chaque fiche produit : six questions générées des faits du modèle (`productFaq` dans `lib/describe.ts` : taille avec le guide de la famille, pratique et niveau, entretien, prix prévu et disponibilité, livraison — matériel lourd à domicile seulement —, retour). Toutes publiées en `FAQPage` : les fiches dans `productGraph`, l’accueil dans `homeGraph`, les services dans le nouveau `serviceGraph` (qui ajoute aussi le `WebPage` qui manquait aux pages de service). Familles, sous-familles et guides en avaient déjà. `scripts/audit-seo.mjs` exige désormais la FAQ visible et le `FAQPage` sur l’accueil, chaque fiche, `/faq/`, `/livraison/`, `/retours/` : 1 105 pages. Mesure en production (alias Vercel, réponses à chaud) : accueil, familles et fiches sous 0,35 s au premier octet, panier sous 0,35 s, `catalogue.json` et `llms.txt` autour de 0,1 s.
- **Index des guides, corrigé le 17 septembre (captures du propriétaire).** Trois causes, trois corrections. (1) Les couvertures avaient une hauteur minimale couplée à leur ratio (`min-height` + `aspect-ratio` dans `app/refinement.css`) : sous 1 100 px la couverture devenait plus large que sa colonne, passait sous la carte voisine (étiquettes jaunes coupées) et, sur téléphone, élargissait toute la page (375 px demandés, 475 px rendus). La hauteur minimale est retirée, la grille passe en `minmax(0, 1fr)` (deux colonnes dès 960 px), l’étiquette et les libellés se dimensionnent sur la largeur de la carte (`container-type: inline-size`, unités `cqi`), la photo garde ses marges avec les deux libellés. Mesuré de 1 440 à 320 px : aucune barre horizontale, aucune étiquette hors de sa carte. (2) Les trois guides ajoutés le 11 septembre n’avaient pas d’entrée dans `lib/data/selection.json` : pas de photo sur la carte, pas de bloc « Avant de choisir » sur la page, pas de photo dans la vignette sociale. Ils ont maintenant chacun leur photo (casque Adidas FFB, protège-dents Shock Doctor, gants Twins 14 oz), leur réponse courte et leurs trois repères reliés aux sections. Au passage, les trois guides MMA qui partageaient la même photo ont chacun la leur : douze couvertures et douze vignettes distinctes. Si une référence est retirée, la carte prend la première photo de la famille liée, jamais une couverture vide. Numérotation « 010 » corrigée en « 10 ». (3) `InvalidStateError: Transition was aborted because of invalid state` : deux éléments pouvaient porter le nom de transition `product-hero` (clic sur une carte depuis une fiche ; page revenue du cache arrière avec le nom encore posé). Le gestionnaire `pageswap` de `components/shop-shell.tsx` ne nomme la photo cliquée que si l’on part vers sa fiche, retire le nom à la photo principale de la page quittée et nettoie au retour (`pageshow`). L’accord `@view-transition` est posé en ligne dans le `<head>` (`app/layout.tsx`) au lieu de deux feuilles de style, avec un garde qui marque comme traitées les promesses d’une transition sautée (onglet masqué, fenêtre redimensionnée). Trouvé en vérifiant : un libellé de clic contenant « ↗ » faisait échouer tout un lot d’événements d’audience sur une base non UTF-8 (500 visible dans la console) ; les flèches et pictogrammes sont retirés des libellés et un lot refusé ne renvoie plus d’erreur au visiteur. Contrôles : lint, TypeScript, API 24, commerce 18, coquille publique, `audit-seo` 1 105 pages.
- **Couverture « LE GANT. » (17 septembre).** La photo Metal Boxe Blade, prise sur fond gris, dessinait un rectangle sur la couverture. Le guide « Comment choisir ses gants de boxe ? » est désormais illustré par la paire Cleto Reyes Pro Sparring rouge (`bs-2000706`, fond blanc, paire entière dans le cadre, seule couverture rouge de la grille) : carte, bloc « Le modèle illustré » et vignette sociale suivent. Douze couvertures distinctes, `audit-seo` 1 105 pages.

## 17 septembre 2026, soir — la scène produit, le billet d’ouverture, le sac en un geste

Brief du propriétaire : « each product has its own background. It's not well put » ; l’inscription doit devenir l’appel à l’action (e-mail obligatoire, mobile facultatif) ; le sac par niveau doit partir au panier d’un seul bouton ; le MMA ne doit plus finir sur « écrivez-nous » ; détourer tout le catalogue ; une vignette de partage par produit, qui montre le produit.

- **Constat mesuré.** Fond des 1 042 photos principales : 687 blanc, 60 gris clair, 39 unis sombres ou colorés, 256 décors ou photos portées. Un tiers du catalogue ne partageait pas le fond des autres ; le `multiply` des cartes ne sauve que le blanc.
- **Détourage du catalogue entier** (`scripts/cutout-images.py`, rembg/u2net en local, rien de téléchargé) : 1 042 références, aucune écartée, 859 objets « posés » (recadrés sur leur silhouette, ramenés à la même échelle, posés sur une ligne de sol à 90 %, ombre d’appui cuite dans l’image) et 183 sujets « cadrés » (photo portée ou plan serré : ancrés au bord qu’ils touchent, sans sol). Bord reculé d’un pixel pour retirer le halo du fond d’origine. Deux tailles WebP dans `public/media/cut/` (86 Mo), manifeste `lib/data/cutouts.json` (mode, teinte dominante, luminance, photo d’origine). `lib/cutouts.ts` attache la découpe au produit côté serveur ; le manifeste ne part jamais dans le navigateur ; une photo remplacée dans l’atelier ne reçoit pas une découpe périmée.
- **La scène commune, déjà en place** (variante « Toile », la plus proche de l’existant) : cartes, sac de séance, photo principale de la fiche, couvertures de guides. Même tapis clair, même lumière, même sol ; un modèle très clair assombrit le tapis d’un cran. Sans découpe, l’ancien traitement reste.
- **Banc d’essai `/labo/`** (hors index, hors plan de site, aucun lien public) : les mêmes 28 modèles réels sur trois scènes — Toile, Teinte (la nuance vient du produit, luminosité tenue, `oklch(from …)`), Ring (noir, projecteur, flaque de lumière, liseré renforcé pour les modèles sombres) — et deux dispositions (grille, rythme : un modèle en grand tous les sept), avec une case « voir les photos d’origine ». Le choix du propriétaire se fait à l’œil.
- **Vignette de partage des fiches** (`app/vignette/[...seg]/route.tsx`) : quand la découpe existe, le produit occupe la moitié droite à fond perdu sur une scène teintée de sa propre couleur, avec le nom, le prix prévu sur ruban jaune et les tailles. Les autres pages gardent leur composition.
- **Billet d’ouverture sur la fiche** : l’inscription passe devant la commande d’essai (bloc sombre, ruban, encoches, ligne de perforation), et la barre d’achat mobile commence par « Me prévenir ». Inscription en deux temps : l’e-mail d’abord (une adresse déjà inscrite ne révèle rien), puis le mobile, facultatif, avec son propre accord, rattaché à l’inscription qui vient d’être créée par une référence rendue une seule fois. Numéro ramené au format international. Colonnes `phone`, `sms_consent`, `source` ajoutées à `alerts` sans étape manuelle (`ensureAlertContact`, et `drizzle/0005`). L’atelier voit le numéro, l’accord et l’endroit de l’inscription. Contrôle ajouté à `scripts/test-api.mjs` (25 contrôles).
- **Le sac de séance part au panier en un geste** (`PackAdd`) : une taille par modèle, un bouton, le total, l’aide qui nomme les tailles manquantes, les modèles déjà possédés exclus. **Enfant en MMA** : deux vrais modèles enfant (rashguard 6-12 ans, protège-dents junior) ; le gant MMA enfant, absent du catalogue, n’est pas remplacé par un gant adulte : une note le dit et propose « prévenez-moi quand ce modèle arrive ».
- **FAQ de l’accueil** : elle n’avait aucune gouttière (l’accueil n’a pas le conteneur des pages) ; elle prend celle des sections, avec une colonne d’introduction.
- Veille et registre des références : `.research/2026-09-17-refonte-produits.md` (ignoré par git).
- **Choix du propriétaire : la scène Ring** (17 septembre, soir). Le noir de la salle, un projecteur, une flaque de lumière, un halo de la couleur du modèle, un liseré pour les modèles sombres : déroulée sur les cartes, le sac de séance, la photo principale des fiches et les vignettes de partage (`app/shop.css`, `app/vignette`). Le banc d’essai `/labo/` reste pour comparer. Sa capture a montré deux défauts invisibles sur fond clair : un voile de fond résiduel (18 découpes) et des fragments (123 : une main coupée par le cadre, une poussière). Le script les traite désormais (courbe de niveaux, élagage par mode) et **écarte** une découpe qui garde moins de 62 % du produit visible (objets fins : élastique, pied de potence) ; la photo d’origine sert alors, posée comme un tirage. Catalogue repassé en entier : 1 034 découpes, 8 écartées, aucun fichier orphelin, 94 Mo.
- **Héros de l’accueil** (`components/hero-ring.tsx`, `app/hero.css`) : un ring. Quatre modèles, un par round (Cleto Reyes rouge, Cleto Reyes noir et or, Blade du club à 17,90 €, casque Adidas FFB), détourés et plus grands que leur cadre ; le projecteur suit le pointeur (il balaie seul sur écran tactile) ; trois cordes au fond, la première en ruban jaune ; le numéro du round en filigrane ; la « fiche de pesée » du modèle (marque, tailles, prix prévu). Le round avance tant que le visiteur ne prend pas la main ; pause au survol, au focus et sur bouton ; rien ne défile en mouvement réduit. Premier bouton de l’accueil : « Me prévenir à l’ouverture ». Le texte d’ouverture du propriétaire est intact.
- **Règle de prix du propriétaire** (`lib/pricing.ts`) : 15 % sous le prix public relevé chez les autres boutiques, **jamais une remise sur une remise**. L’import n’a gardé qu’un prix par référence ; un tarif normal se reconnaît à sa terminaison (,00 ,50 ,90 ,95 ,99), un prix déjà remisé finit n’importe comment (181,30 = 259 − 30 %). Sur 1 023 prix : 906 tarifs normaux baissés de 15,0 à 16,7 % (terminaison ,90 à partir de 50 €, pas de 10 centimes en dessous), 117 prix déjà remisés alignés tels quels. Les fichiers gardent le prix relevé ; la règle s’applique à la lecture. Rapport ligne à ligne : `.research/2026-09-17-regle-de-prix.csv`. Aucun prix barré ni comparaison publique n’est affiché.
- **Synchronisation avec la boutique du club** (`lib/club-sync.ts`, `scripts/check-club-sync.mjs`) : les treize références « mat- » suivent `boutique.boxingcenter.fr/api/materiel` (prix promotionnels compris, jamais en dessous), relu toutes les quinze minutes sans jamais bloquer une page ; un prix aberrant n’est pas appliqué. Écart trouvé et corrigé : les gants Sparring portent « 16 oz ». Un prix saisi dans l’atelier pour une de ces références ne s’affiche pas tant que le club en publie un autre (note dans l’atelier, contrôle dans `test-api`).
- **Vitesse.** Mesure du 17 au soir : le rendu se faisait à Washington (`iad1`) pour des visiteurs entrés par Paris, et un aller-retour vers la base coûtait plus d’une seconde : `vercel.json` fixe `cdg1`. `scripts/check-speed.mjs` tient un budget (temps serveur hors réseau, poids du HTML) ; sa première exécution a trouvé 525 Ko de HTML sur « gants de boxe » et 1,07 Mo sur « matériel de boxe » : la liste entière partait dans la page. Désormais la page envoie les 36 cartes affichées en forme allégée (`listItem`) et la liste entière n’est demandée (`GET /api/catalog-list`) qu’au premier geste : 194 Ko et 198 Ko. Le script de détourage tourne sur quatre fils, en priorité basse, et n’écrit le manifeste qu’à la fin (il faisait recompiler le site en boucle).
- **FFmpeg pour agrandir les photos : écarté, mesure à l’appui.** Un agrandissement 4× (lanczos, avec ou sans accentuation) ramené à la taille d’origine lui est identique à 40 dB ; la netteté au pixel tombe de 1 966 à 9-14 ; le fichier pèse dix fois plus. Aucun détail n’est créé. Les photos locales font 960 px au plus (71 sous 600 px). Deux vraies voies : reprendre les originaux des fournisseurs en pleine taille, ou un modèle de super-résolution (téléchargement à autoriser), cible 1 920 px pour la fiche et le zoom, pas 4K.
- **Concurrent relevé** (matos-de-boxe, Astro, même catalogue Metal Boxe, même état d’avant-vente) : 58 pages contre 1 105, photos servies depuis le CDN du fournisseur, aucune FAQ, aucun `llms.txt`, MCP, `catalogue.json` ni manifeste, vignette sociale générique hors fiches, temps de réponse 0,53-0,67 s, page complète en 9 s. À ne pas copier : à dépasser partout.
- **Mesure en production après la mise en ligne du 17 au soir** (`scripts/check-speed.mjs`, médiane de trois mesures) : accueil 115 ms, « gants de boxe » 96 ms, fiche 79 ms, guides 59 ms, FAQ 67 ms, panier (API) 57 ms, contre 470 à 680 ms deux heures plus tôt. Rendu à Paris confirmé (`cdg1::cdg1`) ; l’aller-retour vers la base passe de 1,3-1,5 s à 0,29 s. HTML de « gants de boxe » : 525 Ko → 176 Ko. Colonnes de contact des alertes créées en production (appel de contrôle sans consentement : 400, rien d’enregistré). Commits `8be4d37` (photos) et `e5b6070`.
- **Héros : le glissement naturel** (retour du propriétaire : « we shouldn't just use the arrows to glide across the product, they should glide naturally »). La piste des modèles est un défilement natif à crans (`scroll-snap`) : doigt, pavé tactile, molette horizontale et glisser à la souris pilotent la même piste que les flèches et les barres de round ; le round affiché se lit sur la position de défilement ; le modèle qui arrive grandit et se redresse en suivant le geste (`animation-timeline: view(inline)`, amélioration progressive). Un glissement n’ouvre pas le lien du modèle. **Mesure d’audience** : un « départ » était envoyé à chaque changement d’onglet (dix pour une seule page vue) ; désormais un par absence réelle de trente secondes, et silence de dix minutes après un 429. **Photos des fournisseurs** : l’original vérifié chez Boxing-Shop fait 700 px, comme notre copie ; l’import n’avait rien réduit, la piste est abandonnée.

## 17 septembre 2026, nuit — le domaine est en ligne sur www, deux corrections immédiates

- **Constat.** Le domaine a été rattaché à Vercel dans la soirée : `https://www.boutique-de-boxe.com` répond 200 ; le domaine nu y redirige (308) et l’alias `boutique-de-boxe.vercel.app` aussi (307). Ce réglage est fait côté Vercel, pas dans le code (le code ne redirige aucun hôte).
- **Adresse canonique alignée sur l’hôte qui répond** : `shop.origin` = `https://www.boutique-de-boxe.com` (canonicals, `og:url`, hreflang, plan de site, graphe, `robots.txt`, `llms.txt`, `catalogue.json`, `mcp.json`, `ai.txt`, `humans.txt`, `security.txt`, IndexNow). Un canonical qui pointe vers une adresse qui redirige se contredit. Si le propriétaire préfère le domaine nu : le passer en « primary » dans Vercel, puis remettre cette constante. `audit-seo` refuse désormais le domaine nu dans les pages ; `check-speed` mesure le domaine.
- **À faire côté Vercel et Supabase** (hors code) : `PAYPLUG_PUBLIC_BASE_URL` et `NEXT_PUBLIC_SITE_ORIGIN` en `https://www.boutique-de-boxe.com` (une notification PayPlug envoyée en POST sur une adresse qui redirige peut se perdre) ; Supabase → Authentication → URL Configuration : Site URL et Redirect URLs sur www.
- **Régression corrigée.** La politique de sécurité posée le matin (`img-src 'self' data:`) bloquait les 1 854 vues secondaires des fiches, servies par les serveurs des fournisseurs (`api.ecommercio.com`, qui redirige vers son stockage, et `media.cdnws.com`). Les trois hôtes sont autorisés ; la vraie correction reste de servir ces photos depuis le site.

## 18 septembre 2026 — pages-piliers, pages de marque, outils MCP

- **Pages-piliers** (`components/keyword-hub.tsx`, `lib/hub.ts`) sur chaque page de famille (première page) et de sous-famille, donc sur les pages des vingt recherches du brief : la **sélection de la semaine** (tirage déterministe page + semaine ISO, une marque par modèle, change chaque lundi, publiée en `ItemList`), **les plus consultés** (mesure maison, sept jours, visites distinctes ; le bloc n’existe que si quatre modèles ont au moins trois visites), **les prix en clair** (par équipement, ou par marque quand la page n’a qu’une famille : nombre de modèles, prix le plus bas, médian, le plus haut) et un **maillage** en quatre colonnes : sous-familles, marques, recherches voisines (la requête exacte en texte de lien), la boutique. Choix du propriétaire : sélection tournante, plus consultés, et choix signés d’un entraîneur (en attente des noms). Jamais de « meilleures ventes » avant des ventes.
- **Pages de marque** (`lib/brands.ts`, `app/marques/`) : un index et 21 pages (au moins six modèles par marque), titre, description, texte d’entrée et cinq questions tirés des faits du catalogue, liste filtrable, tableau de prix par équipement, `CollectionPage` + `ItemList` + `FAQPage`, vignette sociale propre, plan de site, `llms.txt`, index MCP, pied de page. `audit-seo` : **1 127 pages**, 2 561 liens internes.
- **Serveur MCP : 14 outils** (cinq de plus, en lecture seule) : `compare_products`, `recommend_pack` (le sac de séance avec la raison de chaque choix), `recommend_glove_weight` (les repères du guide publié, rien de plus), `get_brands`, `get_weekly_selection`. `mcp.json` liste les quatorze.
